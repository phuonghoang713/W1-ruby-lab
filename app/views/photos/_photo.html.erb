<div class="card" style="width: 20rem;">
  <img class="card-img-top" src="<%= photo.url %>" alt="Card image cap">
  <div class="card-block">
    <h4 class="card-title"><%= photo.username %></h4>
    <%= link_to photo.avatar.filename, photo.avatar%>
    <p class="card-text"> <%= photo.caption %></photo>
    <%= render 'comments', comments: photo.comments %> 
  </div>
</div>
